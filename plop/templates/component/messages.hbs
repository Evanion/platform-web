import { defineMessages } from 'react-intl';

export const scope = '{{{pascalCase name}}}';

export default defineMessages({
  title: {
    id: `${scope}.name`,
    defaultMessage: '{{{pascalCase name}}}',
    description: 'The pagetitle for {{{pascalCase name}}}',
  }
});
