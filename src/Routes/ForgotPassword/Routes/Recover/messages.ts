import { defineMessages } from 'react-intl';

export const scope = 'Recover';

export default defineMessages({
  title: {
    id: `${scope}.PageTitle`,
    defaultMessage: 'Recover',
    description: 'The pagetitle for Recover',
  },
});
